# Function that takes a list and outputs an array with every unique entry coupled with the number of times it occurs in the list.
# Ex: list = [1, 2, 3, 3, 4, 5, 5, 5,]
# print(createDict(list)) = [[1, 1], [2, 1], [3, 2], [4, 1], [5, 3]]
def creatArray(list):
    array = []
    for entry in list:
        pass
    return(array)

# Create a function that writes new data to the data.py thing
